{
  "name": "formica",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "typecheck": "tsgo --noEmit",
    "format": "biome format --write .",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/react": "^19.0.0",
    "react": "^19.0.0"
  }
}
